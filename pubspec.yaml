name: bashlaw_web
description: Bashlaw Global Technologies LTD - Landing Page
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  url_launcher: ^6.2.5
  font_awesome_flutter: ^10.7.0
  google_fonts: ^6.2.1
  animate_do: ^3.3.4
  visibility_detector: ^0.4.0+2
  flutter_svg: ^2.0.10+1
  firebase_core: ^4.7.0
  #smooth_scroll_web: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
